- 股票代码规格化：输入代码自动补全市场前缀（如 sh, sz, bj），并在存储前进行去重处理。
- 配置原子写入：保存配置时先写入临时文件 (.tmp)，成功后再通过 `os.replace` 替换原文件，防止数据损坏。
- 动态列投影：通过独立的布尔属性（如 `price_visible`）控制列显示，Model 层根据可见性动态重组行列数据。
- 资源路径兼容：使用 `resource_path` 函数统一处理开发环境与 PyInstaller 打包后的资源文件路径。